<h1>Editing Review</h1>

<%= render 'form' %>

<%= link_to 'Show', user_product_review_path(@review.product_id, @user.id) %> |
<%= link_to 'Back', user_product_reviews_path(@review.product_id, @user.id) %>
